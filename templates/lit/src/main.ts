import './my-element-lit.ts'

const myElement = document.createElement('my-element-lit')
myElement.setAttribute('id', 'my-element-lit')
// MyElement.innerHTML = '<h1>Vite + Lit</h1>'
document.body.appendChild(myElement)
